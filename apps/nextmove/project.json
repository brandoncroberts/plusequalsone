{
  "root": "apps/nextmove",
  "sourceRoot": "apps/nextmove",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/nextmove",
        "outputPath": "dist/apps/nextmove"
      },
      "configurations": {
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "options": {
        "buildTarget": "nextmove:build",
        "dev": true
      },
      "configurations": {
        "production": {
          "buildTarget": "nextmove:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "nextmove:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/nextmove"],
      "options": {
        "jestConfig": "apps/nextmove/jest.config.js",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/nextmove/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
